# Morphology packs

`starter-pack-v1.json` is an analytic scaffold used to validate the 2D landmark,
deformation, serialization and rendering pipeline. It is **not** presented as a
GNM-derived dataset.

The offline tools in `tools/gnm/` can replace it with a measured pack produced
from neutral GNM Head samples without introducing GNM into the game runtime.
